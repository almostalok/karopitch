import { HomePage } from "@/features/home/HomePage";

/**
 * App Router entrypoint for the landing page.
 * All page logic lives in the feature module for maintainability.
 */
export default function Page() {
  return <HomePage />;
}
